# General

set -ga terminal-overrides ",*256col*:Tc"
set-option -a terminal-overrides ",alacritty:RGB"
set -as terminal-overrides '*:Ss=\E[%p1%d q:Se=\E[2 q'


setw -g xterm-keys on
set -sg repeat-time 600                   # increase repeat timeout
set -s focus-events on

set -g history-limit 5000                 # boost history

# edit configuration
bind e new-window -n "$XDG_CONFIG_HOME/tmux/tmux.conf" sh -c '${EDITOR:-vim} $XDG_CONFIG_HOME/tmux/tmux.conf && tmux source $XDG_CONFIG_HOME/tmux/tmux.conf && tmux display "$XDG_CONFIG_HOME/tmux/tmux.conf sourced"'

# reload configuration
bind r source-file "$XDG_CONFIG_HOME/tmux/tmux.conf" \; display '$XDG_CONFIG_HOME/tmux/tmux.conf sourced'


# Display

set -g base-index 1           # start windows numbering at 1
setw -g pane-base-index 1     # make pane numbering consistent with windows

setw -g automatic-rename on   # rename window to reflect current program
set -g renumber-windows on    # renumber windows when a window is closed

set -g set-titles on          # set terminal title


set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
# set -g @plugin 'jaclu/tmux-power-zoom
set -g @plugin 'dracula/tmux'
set -g @plugin 'tmux-plugins/tmux-pain-control'

set -g status-position top
set -g @dracula-show-flags true
set -g @dracula-show-powerline true
set -g @dracula-refresh-rate 5
# it can accept `session`, `smiley`, `window`, or any character.
set -g @dracula-show-left-icon session
set -g @dracula-show-location false
set -g @dracula-show-timezone false
set -g @dracula-day-month true
set -g @dracula-military-time true
set -g @dracula-plugins "time cpu-usage"


if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

run '~/.tmux/plugins/tpm/tpm'
